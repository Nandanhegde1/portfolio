// NOTE: Koyeb gives you the real URL after first deploy.
// Format: https://<service-name>-<org-slug>.koyeb.app
// Update this line once you have it, then rebuild + redeploy GH Pages.
export const environment = {
  production: true,
  apiUrl: 'https://nandan-portfolio-api.koyeb.app',
};
